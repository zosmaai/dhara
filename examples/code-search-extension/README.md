# code-search

Dhara extension for code search. Uses ripgrep (falls back to grep) and fd (falls back to find).

**Tools:** `code_search` (regex search), `file_find` (glob search)
**Capabilities:** `process:spawn`

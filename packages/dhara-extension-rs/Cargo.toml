[package]
name = "dhara-extension"
version = "0.1.0"
description = "Rust SDK for building Dhara extensions"
edition = "2021"
license = "MIT"
repository = "https://github.com/zosmaai/dhara"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
